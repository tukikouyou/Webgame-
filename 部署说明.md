# 弹珠领土战争 — 部署说明（GitHub Pages 自动上线）

本项目是 **Vite + TypeScript** 工程，浏览器不能直接跑 `.ts` 源码，
必须先**构建**（`npm run build`）成普通 HTML + JS 才能上线。
好在这个「构建 → 发布」的过程可以用 **GitHub Actions 全自动完成**：

> **推代码到 `main` → 自动构建 → 网页更新**，中间你什么都不用管。

线上地址：**https://tukikouyou.github.io/Webgame-/**

---

## 一、原理（为什么要构建）

| | 以前（单 HTML） | 现在（TS 工程） |
|---|---|---|
| 本地开发 | 双击打开 | `npm run dev`（热更新） |
| 部署上线 | 直接传 HTML | `npm run build` → 发布 `dist/` |
| 自动化 | 能 | **能**（GitHub Actions 自动构建） |

`npm run build` 会在 `dist/` 里生成纯 HTML + JS（约 48 KB），
任何静态托管都能直接跑；`dist/` 不进 git（已在 `.gitignore`），由 CI 每次自动生成。

---

## 二、已经配好的东西

| 文件 | 作用 |
|------|------|
| `vite.config.ts` | `base: './'` —— 用相对路径打包，使产物在 GitHub Pages 子路径 `/Webgame-/` 下也能正常加载资源（否则白屏）。 |
| `.github/workflows/deploy.yml` | 部署工作流：推到 `main` 时自动 `npm ci → npm run build → 发布 dist/`。也能在 Actions 页手动触发。 |

---

## 三、启用步骤

### 1. 一次性设置（只能在 GitHub 网页操作）
仓库 → **Settings → Pages → Build and deployment → Source** 选 **「GitHub Actions」**。
（只需做这一次，以后不用再管。）

### 2. 把代码放到 `main` 分支
工作流只在 `main` 上触发。把当前改动提交并合并 / 推送到 `main`：

```bash
git add -A
git commit -m "重构为 Vite+TS 工程 + GitHub Pages 自动部署"
git checkout main
git merge docs/project-structure     # 或用 PR 合并
git push origin main
```

### 3. 等待自动部署
推送后进仓库 **Actions** 页可看到工作流在跑（约 1~2 分钟）。
绿勾后打开 **https://tukikouyou.github.io/Webgame-/** 即是最新版。

之后每次改完代码，只要 `git push` 到 `main`，网页就会自动更新。

---

## 四、本地开发常用命令

```bash
npm install        # 首次拉取项目后装依赖
npm run dev        # 开发服务器，浏览器开 http://localhost:5173（热更新）
npm run build      # 生产构建（tsc 类型检查 + vite 打包）→ dist/
npm run preview    # 本地预览构建产物
npm run typecheck  # 只跑 TypeScript strict 类型检查
```

> 本机 Node.js 装在 `C:\Program Files\nodejs\`。若新终端里 `node`/`npm` 找不到，
> 在 PowerShell 命令前刷新 PATH：
> ```powershell
> $env:Path = [Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [Environment]::GetEnvironmentVariable("Path","User")
> ```

---

## 五、常见问题

**Q：打开线上网页白屏 / 资源 404？**
A：多半是资源路径问题。确认 `vite.config.ts` 里有 `base: './'`；改完要重新构建、重新部署。

**Q：Actions 报错 `npm ci` 失败？**
A：确保 `package-lock.json` 已提交到仓库（`npm ci` 依赖它）。若确实缺失，本地跑一次 `npm install` 生成后提交。

**Q：推了代码但网页没更新？**
A：① 确认推的是 `main` 分支；② 去 Actions 页看工作流是否成功（绿勾）；③ 浏览器强制刷新（Ctrl+F5）清缓存。

**Q：想要一个不用构建、双击就能跑的版本？**
A：项目里保留了原单文件 `marble_territory_war.html`，双击即可运行，可作应急/分享用。

---

## 六、和将来「上微信小游戏 / Steam」的关系

现在这套 GitHub Pages 部署走的是 **Web 端**。将来：
- **微信小游戏**：把 `render/`+`ui/` 适配到微信 Canvas，`core/`+`config/` 直接复用，另走微信开发者工具上传，不走这套 Pages 流程。
- **Steam**：用 Cocos/C# 或 Electron 打包成桌面可执行文件，也不走 Pages。

也就是说，**GitHub Pages 是现阶段「网页试玩 / 分享 demo」用的**，和后面两个正式发布渠道并行、互不冲突。
