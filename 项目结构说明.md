# 弹珠领土战争 — 项目结构说明

> 本项目已从单文件 HTML 重构为 **Vite + TypeScript** 分层工程。
> 目标路线：先上**微信小游戏**，内容做厚后用 **C#** 上 **Steam**。
> `core/` 是引擎无关的「可移植内核」，将来换 Cocos / C# 时它照着翻译即可，只需重写 `render/` 和 `ui/`。

---

## 一句话总览

**`ui/` 收用户操作 → 改 `core/` 的状态 → `main.ts` 每帧让 `core/` 推进一步、让 `render/` 把状态画到 Canvas。配置全部来自 `config/`。**

```
数据流:   用户点击 ──▶ ui/ ──┐
                            ├──▶ core/ (GameState 状态)
          config/ 常量 ─────┘         │
                                      ▼
          main.ts: 每帧 step(推进) + render(绘制)
                                      │
                                      ▼
                              render/ ──▶ <canvas>
```

**四层互不越界**（这是将来能顺利搬去微信小游戏和 C# 的根本原因）：
- 想改**数值** → `config/`
- 想改**玩法** → `core/`
- 想改**画面** → `render/`
- 想改**按钮/界面** → `ui/`

> 硬性约束：`core/` 和 `config/` 里**禁止出现** `document` / `window` / `canvas` / `ctx` 等 DOM/Canvas 引用。这是「可移植内核」的保证。

---

## 📁 config/ —— 数据化配置（改数值只动这里）

| 文件 | 作用 |
|------|------|
| `tuning.json` | 所有**数值常量**：格子数、炮台血量、各种速度/半径/时间。调平衡的总开关。 |
| `teams.json` | 四个队伍的**名字/颜色/所在角**。 |
| `traits.json` | 10 个**专属特性**的名字和说明文字。 |
| `skills.json` | 转盘**技能标签**、默认特性、炮台默认参数。 |
| `configTypes.ts` | 上面这些 JSON 的**类型定义**（TeamDef/TraitDef… → 将来 C# 的结构体）。 |
| `config.ts` | 读入 JSON + **算派生常量**（如单格尺寸 CS、钉板/转盘节点坐标），统一导出给 core 和 render 用。 |

## 📁 core/ —— 纯逻辑内核（零 Canvas/DOM，将来可移植 C#/Cocos）

| 文件 | 作用 |
|------|------|
| `types.ts` | **`GameState` + 所有实体接口**（炮台/弹珠/各种技能弹…）。整个项目的数据骨架。 |
| `state.ts` | **建局与重置**：`createState` / `resetState`，铺初始领地、造四炮台、发初始弹珠。 |
| `step.ts` | **单帧总编排**：按固定顺序调用下面所有 update，是主循环的心脏。 |
| `grid.ts` | **格子数据操作**：setCell/paintCell 只改数据 + 标记「脏格」，**不画图**（关键解耦点）。 |
| `cannons.ts` | 炮台行为：摆炮口、开火，以及环卫/回血/固土/增殖/游牧等**特性逻辑**。 |
| `marbles.ts` | 普通小球物理：撞墙/护盾/挡板/格子/炮台的碰撞结算、相位弹珠互撞。 |
| `plinko.ts` | 左上**弹珠面板**：小球下落、落入 RELEASE/MULTIPLY/SPIN 的效果。 |
| `wheel.ts` | 右上**普通技能转盘**：小球弹跳、触发 9 个技能。 |
| `ultimate.ts` | 右下**终极转盘**：触发 7 个终极技能。 |
| `projectiles.ts` | 五种**技能弹体**：炸弹/追踪弹/核弹+冲击波/贯穿弹的发射与推进。 |
| `damage.ts` | **共享战斗原语**：伤害入口、淘汰、护盾、挡板命中、磁场、胜负判定。 |
| `fx.ts` | **特效缓冲**：逻辑往 state 写粒子/爆炸/toast/闪光（渲染只读）。 |
| `rng.ts` | **可置种随机数**，让逻辑可复现、且对应 C# 同款实现。 |
| `util.ts` | 纯工具函数：角度归一化 `norm`、数字格式化 `fmt`。 |

## 📁 render/ —— 渲染层（只读 state，往 Canvas 画）

| 文件 | 作用 |
|------|------|
| `renderer.ts` | **总渲染**：每帧按图层顺序调用下面所有 draw。 |
| `grid.ts` | 离屏格子画布，每帧**只重绘脏格**（配合 core/grid 的解耦，保性能）。 |
| `drawArena.ts` | 画战场：小球、各技能弹、炮台、护盾、血条、HUD、冲击波。 |
| `drawPanels.ts` | 画左上面板 + 两个转盘。 |
| `drawFx.ts` | 画粒子、顶部 toast、结算画面。 |
| `colors.ts` | 颜色工具：调明暗 `shade`、派生领地色、颜色名。 |

## 📁 ui/ —— 界面层（只操作 DOM）

| 文件 | 作用 |
|------|------|
| `controls.ts` | 顶栏按钮：暂停/重开/倍速/打开选择或调试面板。 |
| `setup.ts` | **开局选择界面**：给四炮台选特性、自选颜色。 |
| `debug.ts` | **调试面板**：实时拖动改每个炮台的血量/弹药/物理参数等。 |

## 📄 根目录关键文件

| 文件 | 作用 |
|------|------|
| `main.ts` | **入口**：拿 canvas、建 state、接 UI、跑 RAF 主循环（每帧 step + render）。 |
| `appTypes.ts` | `App` 上下文类型（暂停/倍速/新局等），给 UI 层用。 |
| `index.html` | 页面骨架：`<canvas>` + 顶栏/弹窗 DOM + 样式。 |
| `package.json` | 依赖与脚本（dev / build / typecheck）。 |
| `tsconfig.json` | TypeScript strict 配置。 |
| `marble_territory_war.html` | **原单文件版**，保留作行为对照基准，不参与运行。 |

---

## 常用命令

```bash
npm run dev        # 启动开发服务器，浏览器开 http://localhost:5173
npm run build      # 生产构建（tsc 类型检查 + vite 打包）
npm run typecheck  # 只跑 TypeScript strict 类型检查
```

> 本机 Node.js 装在 `C:\Program Files\nodejs\`。若新终端里 `node`/`npm` 找不到，
> 在 PowerShell 命令前刷新 PATH：
> `$env:Path = [Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [Environment]::GetEnvironmentVariable("Path","User")`

---

## 加新功能时该动哪里？

| 需求 | 改哪 |
|------|------|
| 调平衡（血量、速度、时间…） | `config/tuning.json` |
| 加/改一个专属特性 | `config/traits.json` + `core/cannons.ts`（或相应逻辑模块） |
| 加/改一个转盘技能 | `config/skills.json` + `core/wheel.ts` / `core/ultimate.ts` / `core/projectiles.ts` |
| 改某个东西的画法 | 对应的 `render/draw*.ts` |
| 加界面控件 | `ui/` + `index.html` |

**原则**：改玩法尽量只动 `core/`，数值进 `config/*.json`，别让 `core/` 碰 DOM —— 这样将来迁移微信小游戏 / C# 时最省力。
